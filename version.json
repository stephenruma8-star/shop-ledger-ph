{
    "version":  "3.5.0",
    "buildDate":  "2026-08-19T00:00:00.000Z",
    "notes":  "Storage engine migrated from IndexedDB to SQLite (better-sqlite3): records are kept in a local .sqlite database with WAL mode for faster lookups and crash-safe writes. Your existing data migrates automatically on first launch; the IndexedDB fallback remains for edge cases. Includes a rebuild helper (npm run rebuild:electron) for testing with the Electron binary in dev."
}
